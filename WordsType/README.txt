# 墨读字体目录 WordsType

## 这是什么？
存放字体文件的目录。墨读启动时会自动扫描此目录，将新增的 `.ttf` / `.otf` 字体安装到系统中。

## 打包后（安装版）
进入墨读 **设置 → 高级设置** → 点击 **"打开字体目录"** 按钮
→ 把字体文件放进去 → 重启墨读即可

## 开发时
直接放在项目根目录的 `WordsType/` 文件夹里。

> 支持任意 TTF / OTF 字体，不限本文中列出的字体。
